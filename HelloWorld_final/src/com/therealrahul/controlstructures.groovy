//if - if evaluates what's in the pharenthesis
if( true ) 
    println "value is true"

// false | null empty strings | empty collections
if( false ) //this wont return anything because after the value is false then groovy doesn't go to the second line
    println "value is false"